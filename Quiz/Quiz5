#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	// 구구단 2단부터 Full로 출력하라.

	for (int i = 2; i < 10; i++)
	{
		for (int j = 1; j < 10; j++) {
			printf("%d * %d = %d\n", i, j, i * j);
		}
		printf("--------------------\n");
	}

	return 0;
}
